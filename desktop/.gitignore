node_modules
dist
out
release
.vite
tmp

# Never commit client data — the old repo leaked the live SQLite DB, backups and logos.
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite-wal
*.sqlite-shm
/userdata

# Local env
.env
.env.local

# Logs
*.log
logs

# Editor
.DS_Store
.idea
*.suo
*.njsproj
*.sln

# Comparison fixture: derived from the client's real database, so it carries
# their company and buyer details. Regenerate locally, never commit.
scripts/fixture-invoice.json
scripts/.tmp-orig
